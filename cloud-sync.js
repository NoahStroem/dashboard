/* ============================================================
 * cloud-sync.js — DEPRECATED. Replaced by sync-ui.js.
 *
 * The ☁ button became a proper status indicator: Syncing / Synced / Offline /
 * Sync failed, with the last-synced time, the pending-change count, a retry and
 * a manual sync. Pages load sync-ui.js instead.
 *
 * Kept as a no-op so a page served from an old HTTP cache doesn't 404 on it.
 * See SYNC.md.
 * ============================================================ */
/* intentionally empty */
